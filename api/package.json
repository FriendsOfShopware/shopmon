{
  "name": "shopmon",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "esbuild --bundle --outfile=worker.js src/index.ts",
    "deploy:worker": "wrangler publish",
    "deploy:site": "cd frontend && npm run build && wrangler pages publish --project-name shopmon  dist/"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@planetscale/database": "^1.3.0",
    "bcryptjs": "^2.4.3",
    "itty-router": "^2.6.1",
    "shopware-app-server-sdk": "^0.0.15"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.14.1",
    "@types/bcryptjs": "^2.4.2",
    "esbuild": "^0.15.5",
    "typescript": "^4.7.4"
  }
}
